# Скрипт для сравнения двух версий APK

**APK Diff Inspector** — это утилита для детального сравнения двух версий APK-файлов. Скрипт позволяет выявить изменения в разрешениях, компонентах приложения, манифесте, фичах, размерах и потенциальных рисках безопасности.

---

## 📌 Возможности

`inspector.py` анализирует два APK-файла и строит подробный diff-отчёт, включающий:

### 🔍 Meta / Мета-информация
- Package Name  
- versionName / versionCode  
- minSdkVersion  
- targetSdkVersion  

### 🔐 Permissions / Разрешения
- Добавленные  
- Удалённые  
- Неизменённые  
- Выявление новых dangerous-разрешений

### 🧩 Components / Компоненты
Анализируются такие сущности:
- Activities  
- Services  
- Broadcast Receivers  
- Content Providers  

Учитываются:
- `exported=true/false`  
- наличие intent-filters  
- изменения конфигурации компонентов  

### ⚙️ Features / Аппаратные и системные фичи
- Новые и удалённые `uses-feature`  
- Изменения обязательности (`required=true/false`)

### 📦 Sizes / Размеры
- Общий размер APK  
- Размеры и количество:
  - DEX  
  - native-библиотек (lib/)  
  - ресурсов (res/)  
  - ассетов (assets/)  
- Список ABI (armeabi-v7a, arm64-v8a и др.)

### 🚨 Security Report / Потенциальные риски
Скрипт автоматически выявляет:
- Новые dangerous permissions  
- Новые или изменённые `exported` компоненты  
- Компоненты, которые могут быть уязвимы  

---

## 📁 Пример использования

### Базовый анализ двух APK
```bash
python inspector.py old.apk new.apk
````

### Только security-изменения

```bash
python inspector.py old.apk new.apk --danger-only
```

### Вывод в JSON

```bash
python inspector.py old.apk new.apk --json > report.json
```

### Сохранить отчёт в файл

```bash
python inspector.py old.apk new.apk -o diff.json
```

---

## 📥 Установка

Установите Python 3.8+.

Склонируйте репозиторий:

```bash
git clone https://github.com/Android-RU/APK-Diff-Inspector
cd APK-Diff-Inspector
```

После чего можно запускать:

```bash
python inspector.py old.apk new.apk
```

---

## 🔧 Зависимости

Скрипт работает из коробки, без дополнительных библиотек. Но если установлен [`androguard`](https://github.com/androguard/androguard), `inspector.py` будет использовать его для более точного парсинга `AndroidManifest.xml`.

Установка (необязательно):

```bash
pip install androguard
```

---

## 🧱 Структура проекта

```
APK-Diff-Inspector/
│── inspector.py   # Основной скрипт
│── README.md      # Документация
└── LICENSE        # MIT лицензия
```

---

## 📄 Лицензия

Проект распространяется по лицензии **MIT**. Вы можете свободно использовать, модифицировать и распространять код. Полный текст лицензии находится в файле `LICENSE`.
